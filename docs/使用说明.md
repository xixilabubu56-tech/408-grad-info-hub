# 408 研招信息库使用说明

## 1. 项目简介

本项目用于查询全国统考 408 相关的研究生招生信息，当前项目由三部分组成：

- 前端：React + Vite，负责网页展示
- 后端：FastAPI，负责提供接口
- 数据脚本：Python + MySQL，负责初始化和更新数据

当前项目默认使用本地 MySQL 数据库，前端通过后端接口读取数据库中的院校、专业、历年数据和通知信息。

---

## 2. 目录说明

```text
information/
├── backend/              后端接口
├── frontend/             前端网页
├── scraper/              数据初始化与更新脚本
├── docs/                 文档
└── README.md             项目总说明
```

最重要的几个文件：

- `backend/.env`：数据库连接配置
- `backend/init_db.py`：初始化数据库表结构
- `scraper/sync_408_data.py`：推荐的统一数据同步入口
- `scraper/seed_comprehensive_408.py`：核心 50 校数据同步脚本
- `backend/main.py`：后端启动入口

---

## 3. 环境准备

建议环境：

- macOS
- Python 3.12+ 或当前项目已创建的 venv
- Node.js 18+
- npm 9+
- MySQL 8.x

如果你已经按之前步骤安装好了 Homebrew 和 MySQL，可以直接继续下面的配置。

---

## 4. 第一次使用：完整启动流程

### 4.1 配置 MySQL

先确认 MySQL 已启动：

```bash
mysql.server start
```

然后进入 MySQL：

```bash
mysql -u root -p
```

如果你的 root 没密码，也可以：

```bash
mysql -u root
```

创建数据库：

```sql
CREATE DATABASE IF NOT EXISTS grad_info_408 DEFAULT CHARACTER SET utf8mb4;
```

创建完成后退出：

```sql
exit
```

---

### 4.2 配置后端数据库连接

打开 [backend/.env](file:///Users/macbook/Desktop/408project/information/backend/.env)。

当前默认配置是：

```env
DATABASE_URL=mysql+pymysql://root:@localhost:3306/grad_info_408?charset=utf8mb4
```

如果你的 MySQL 有密码，请改成：

```env
DATABASE_URL=mysql+pymysql://root:你的密码@localhost:3306/grad_info_408?charset=utf8mb4
```

例如：

```env
DATABASE_URL=mysql+pymysql://root:123456@localhost:3306/grad_info_408?charset=utf8mb4
```

---

### 4.3 安装后端依赖

进入后端目录：

```bash
cd /Users/macbook/Desktop/408project/information/backend
```

如果你还没创建虚拟环境：

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

如果已经有虚拟环境，直接：

```bash
cd /Users/macbook/Desktop/408project/information/backend
source venv/bin/activate
pip install -r requirements.txt
```

---

### 4.4 初始化数据库表

在后端目录执行：

```bash
cd /Users/macbook/Desktop/408project/information/backend
source venv/bin/activate
python init_db.py
```

这一步会创建数据库表，包括：

- institutions
- majors
- historical_data
- official_notices

---

### 4.5 导入初始数据

进入爬虫目录：

```bash
cd /Users/macbook/Desktop/408project/information/scraper
```

如果 scraper 目录还没有安装依赖：

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

然后执行推荐的数据同步脚本：

```bash
cd /Users/macbook/Desktop/408project/information/scraper
source venv/bin/activate
python sync_408_data.py
```

当前这一步会导入：

- 50 所院校
- 113 个专业
- 339 条历年数据
- 150 条通知

如果导入成功，终端会看到类似输出：

```bash
✅ 已同步 50 所目标院校
✅ 已写入 113 个专业
✅ 已写入 339 条历年数据
✅ 已写入 150 条官方通知
```

---

### 4.6 启动后端

进入后端目录启动接口：

```bash
cd /Users/macbook/Desktop/408project/information/backend
source venv/bin/activate
uvicorn main:app --reload --port 8000
```

启动成功后，后端接口地址为：

```text
http://127.0.0.1:8000
```

可以在浏览器访问：

- 首页接口：`http://127.0.0.1:8000/`
- 健康检查接口：`http://127.0.0.1:8000/api/health`
- 院校接口：`http://127.0.0.1:8000/api/institutions`
- 摘要接口：`http://127.0.0.1:8000/api/summary`

---

### 4.7 启动前端

进入前端目录：

```bash
cd /Users/macbook/Desktop/408project/information/frontend
```

安装依赖：

```bash
npm install
```

启动开发环境：

```bash
npm run dev -- --host 127.0.0.1 --port 4173
```

浏览器访问：

```text
http://127.0.0.1:4173
```

---

## 5. 最常用的启动命令

如果你以后已经配置完成，只需要记住下面几组命令。

### 5.1 启动 MySQL

```bash
mysql.server start
```

### 5.2 启动后端

```bash
cd /Users/macbook/Desktop/408project/information/backend
source venv/bin/activate
uvicorn main:app --reload --port 8000
```

### 5.3 启动前端

```bash
cd /Users/macbook/Desktop/408project/information/frontend
npm run dev -- --host 127.0.0.1 --port 4173
```

### 5.4 打开网页

```text
http://127.0.0.1:4173
```

---

## 6. 数据怎么更新

当前项目的数据更新方式分为两类。

### 6.1 重新导入当前完整数据

如果你只是想把数据库同步到当前项目内置的 50 校数据，执行：

```bash
cd /Users/macbook/Desktop/408project/information/scraper
source venv/bin/activate
python sync_408_data.py
```

注意：

- 默认是安全同步模式，会按院校覆盖更新，不会先清空整个数据库
- 如果你确实要从空库重建，可执行：

```bash
python sync_408_data.py --reset
```

---

### 6.2 可选的扩展同步方式

当前项目已经把数据同步入口统一为 `sync_408_data.py`，你目前可以这样理解：

- `sync_408_data.py`：正式推荐入口，统一负责建表、同步核心数据、打印统计
- `seed_comprehensive_408.py`：核心结构化数据同步脚本
- `fetch_and_save.py`：研招网院校列表实验同步
- `fetch_408_details.py`：408 详情样例同步模板

所以目前正式推荐的更新方式是：

```bash
python sync_408_data.py
```

如果你想附带执行额外的实验同步，可以用：

```bash
python sync_408_data.py --with-yzw-list --with-detail-demo
```

---

## 7. 怎么和 MySQL 对接

### 7.1 当前对接方式

项目通过 SQLAlchemy + PyMySQL 连接 MySQL。

配置文件在：

- [database.py](file:///Users/macbook/Desktop/408project/information/backend/database.py)
- [backend/.env](file:///Users/macbook/Desktop/408project/information/backend/.env)

连接字符串格式：

```env
DATABASE_URL=mysql+pymysql://用户名:密码@主机:端口/数据库名?charset=utf8mb4
```

例如：

```env
DATABASE_URL=mysql+pymysql://root:123456@localhost:3306/grad_info_408?charset=utf8mb4
```

---

### 7.2 怎么确认已经连上 MySQL

你可以运行：

```bash
cd /Users/macbook/Desktop/408project/information/backend
source venv/bin/activate
python init_db.py
```

如果看到数据库表创建成功，说明连接正常。

你也可以直接登录 MySQL 查看：

```bash
mysql -u root -p
```

然后执行：

```sql
USE grad_info_408;
SHOW TABLES;
SELECT COUNT(*) FROM institutions;
```

---

### 7.3 常用数据库检查语句

```sql
USE grad_info_408;

SHOW TABLES;

SELECT COUNT(*) FROM institutions;
SELECT COUNT(*) FROM majors;
SELECT COUNT(*) FROM historical_data;
SELECT COUNT(*) FROM official_notices;

SELECT id, name, ranking FROM institutions ORDER BY ranking ASC LIMIT 10;
```

---

## 8. 常见问题

### 8.1 `npm run build` 报 `ENOENT`，找不到 package.json

原因：你在根目录执行了 npm 命令。

错误目录：

```bash
cd /Users/macbook/Desktop/408project/information
npm run build
```

正确目录：

```bash
cd /Users/macbook/Desktop/408project/information/frontend
npm run build
```

因为只有 `frontend/` 目录下才有 `package.json`。

---

### 8.2 前端能打开，但没有数据

先检查三件事：

1. MySQL 是否启动
2. 后端是否启动在 8000 端口
3. 数据是否已经导入

建议顺序：

```bash
mysql.server start
cd /Users/macbook/Desktop/408project/information/backend
source venv/bin/activate
uvicorn main:app --reload --port 8000
```

另开一个终端：

```bash
cd /Users/macbook/Desktop/408project/information/scraper
source venv/bin/activate
python seed_comprehensive_408.py
```

---

### 8.3 8000 端口被占用

可以先查占用进程：

```bash
lsof -i :8000
```

强制结束：

```bash
lsof -ti:8000 | xargs kill -9
```

然后重新启动后端。

---

### 8.4 MySQL 连不上

先确认 MySQL 是否启动：

```bash
mysql.server start
```

再确认配置文件中的用户名、密码、数据库名是否正确：

- [backend/.env](file:///Users/macbook/Desktop/408project/information/backend/.env)

---

### 8.5 页面还是旧数据

可能原因：

- 后端没重启
- 浏览器缓存还在
- 数据脚本没重新执行

建议：

1. 重新运行 `python sync_408_data.py`
2. 重启后端
3. 刷新浏览器

---

## 9. 推荐的日常使用流程

每次想正常使用这个系统，推荐按下面顺序：

### 方式一：只查看网页

```bash
mysql.server start
cd /Users/macbook/Desktop/408project/information/backend
source venv/bin/activate
uvicorn main:app --reload --port 8000
```

再开一个终端：

```bash
cd /Users/macbook/Desktop/408project/information/frontend
npm run dev -- --host 127.0.0.1 --port 4173
```

打开：

```text
http://127.0.0.1:4173
```

---

### 方式二：先更新数据，再查看网页

```bash
mysql.server start
```

```bash
cd /Users/macbook/Desktop/408project/information/scraper
source venv/bin/activate
python sync_408_data.py
```

```bash
cd /Users/macbook/Desktop/408project/information/backend
source venv/bin/activate
uvicorn main:app --reload --port 8000
```

```bash
cd /Users/macbook/Desktop/408project/information/frontend
npm run dev -- --host 127.0.0.1 --port 4173
```

最后访问：

```text
http://127.0.0.1:4173
```

---

## 10. 当前项目的实际状态

当前版本已经可以做到：

- 启动前后端网页
- MySQL 持久化存储
- 50 所院校展示
- 多学院、多专业展示
- 历年数据与复录比展示
- 通知展示
- 研究生院与学院官网入口展示
- 院校页关键词搜索、排序、学位筛选、院校属性筛选
- 详情页学院切换、错误重试、最新年份与最近通知日期展示
- 健康检查接口与更安全的数据库同步流程

当前仍建议你这样使用：

- 日常展示：前端 + 后端直接启动
- 数据刷新：运行 `sync_408_data.py`
- 数据核对：通过学校官网和 MySQL 手动检查

---

## 11. 一句话版

如果你只想最快跑起来，就执行下面四步：

```bash
mysql.server start
```

```bash
cd /Users/macbook/Desktop/408project/information/scraper
source venv/bin/activate
python sync_408_data.py
```

```bash
cd /Users/macbook/Desktop/408project/information/backend
source venv/bin/activate
uvicorn main:app --reload --port 8000
```

```bash
cd /Users/macbook/Desktop/408project/information/frontend
npm run dev -- --host 127.0.0.1 --port 4173
```

然后打开：

```text
http://127.0.0.1:4173
```
